# Python : Fichiers compilés et caches
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.coverage
*.cpython*

# Environnement virtuel (si tu en crées un)
venv/
.env

# Système Mac
.DS_Store

# IDE (VS Code / Cursor)
.vscode/

#Logs
logs/
*.log

#tiled
*.tiled-session

# Player saves (local only — never commit)
saves/

# Sentrux — generated baseline (re-create with: sentrux gate --save .)
.sentrux/baseline.json
pyright_output.txt
.tdd_lock
verify_output.json
.sc-*
.specconformance
.agents/cross-spec.config.yml
.agents/cross_spec_report.md
saves_migrated/
!saves_migrated/.gitkeep