# Intent Trace — business assertions with deterministic proofs
# Created at: STRATEGY stage (assertions only)
# Updated at: SPEC stage (proof + rationale attached)

version: "1"

assertions:
  - id: "IA-001"
    assertion: "The player can ascend and descend the basement stairs and return to the exact starting logical coordinates without Y-coordinate drift."
    rationale: "The integration tests verify that traversing the stairs up and down returns the entity to the exact starting logical coordinates, ensuring step symmetry."
    scope: code
    proof:
      type: test_exit_code
      reference: "game/tests/integration/test_stairs_integration.py"
      expected_exit: 0
    files:
      - "game/src/entities/base.py"
    status: null

  - id: "IA-002"
    assertion: "The player sprite is rendered at the correct visual height corresponding to the stair graphic assets during all stages of movement and while standing still."
    rationale: "The unit tests verify that current_stair_offset is updated statically when standing still and is smoothly interpolated between starting and target offsets during movement progress."
    scope: code
    proof:
      type: test_exit_code
      reference: "game/tests/entities/test_stair_movement.py"
      expected_exit: 0
    files:
      - "game/src/entities/base.py"
      - "game/src/entities/groups.py"
    status: null

  - id: "IA-003"
    assertion: "An entity on a ladder is restricted to vertical (Up/Down) movement only, ignoring horizontal inputs."
    rationale: "Unit tests verify that pressing Left/Right on a ladder tile blocks movement and resets direction, while Up/Down inputs trigger vertical movement."
    scope: code
    proof:
      type: test_exit_code
      reference: "game/tests/entities/test_ladder_movement.py"
      expected_exit: 0
    files:
      - "game/src/entities/base.py"
    status: null

  - id: "IA-004"
    assertion: "An entity on a tile with clip = true has its sprite bottom-cropped by the absolute value of visual_y_offset (defaulting to 8, clamped to sprite height), and the crop amount interpolates smoothly during movement."
    rationale: "Unit tests in test_stair_movement.py verify that BaseEntity.current_stair_clip is updated when standing still and is smoothly interpolated during movement, and that CameraGroup.custom_draw applies the blit area crop when a non-zero clip is present."
    scope: code
    proof:
      type: test_exit_code
      reference: "game/tests/entities/test_stair_movement.py"
      expected_exit: 0
    files:
      - "game/src/entities/base.py"
      - "game/src/entities/groups.py"
    status: null




